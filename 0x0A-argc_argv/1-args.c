#include <stdio.h>

/**
* main - Entry point
*
* @argc: number of arguments
*
* @argv: list of arguments
*
* Return: (Always/Success)
*/

int main(int argc, char **argv)
{
printf("%d\n", argc);
if (argv[0] == argv[1])
{
return (0);
}
return (0);
}
