#include <stdio.h>

/**
* main - Entry point
*
* @argc: number of arguments
*
* @argv: list of arguments
*
* Return: (Always/Success)
*/

int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
if (argc < 0)
{
printf("%s\n", argv[0]);
}
return (0);
}
